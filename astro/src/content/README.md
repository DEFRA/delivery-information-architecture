# Content Directory

⚠️ **Important**: The `docs/` directory here is a **symlink** to `../../../docs`.

**Do not edit files in this directory directly.** All documentation should be edited in the `/docs` directory at the repository root.

The symlink exists only to allow Astro's content collection system to read the documentation files. The actual source of truth is in `/docs`.
